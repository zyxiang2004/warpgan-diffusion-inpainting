# PREREG_V18W3 — InvSR 校准监督 + 结构对照（起跑前签署，2026-09-11）

> 前史：wave-1（全 t 段重加噪对 + FM×100/adv×10）被肉眼终审证伪（输出纯斑
> 点，val 从未离开未训练水平）；机理=高 t 段参照物自身是噪声（实测
> train_logs/diag_pair_domain.png），外观监督只在 t≲300 有内容。
> 本轮按 InvSR（CVPR 2025, arXiv 2412.09013，代码逐行核对）的发表配方
> 重建分布监督，并同步完成 D 结构对照。

## 1. 两臂（GPU 1/2 并行，各 30,000 步，从零，同代码同数据同种子）

| 臂 | GPU | 配方 | 回答的问题 |
|---|---|---|---|
| **W3-D** | 1 | InvSR 监督 + 真实批 pass1 **关闭**（原版 FFC 对 pred_novel 零监督的忠实语义；pass2 bank=[warp_warp, x_mirror 照片]） | 四组件组装的完整检验：质感与空洞内容能否同时成立 |
| **W3-P** | 2 | 同 W3-D 但 pass1 **开启**（全带 anchor eps） | 单变量对照：pass1 锚在强 pass2 下是帮手还是毒药（CTRL 证明弱 pass2 下是油画） |

**InvSR 校准监督**（两臂相同，全部照抄其发表代码，出处标注在
`_x0_losses_invsr` docstring）：
- 域：t≤300（实测有效边界；InvSR 用 [100,250]）
- 对象：单步 x0 估计，潜码 clamp ±10（其 _Latent_bound）
- 损失（**全部潜空间，无 VAE 解码**）：MSE(z0̂,target)×1.0 ＋ FM(D 头特征)×2.0
  （顶替其不可离线获取的微调潜 LPIPS）＋ adv×0.1（仅 3K 预热后）
- 判别器：106M 多尺度 UNet-D（投影输入条件化 + 时间步条件——防 v14 噪声
  捷径），hinge 损失，每步更新，Adam 5e-5/wd1e-3
- target：pass2=真实照片潜码；synth=配对渲染潜码（同视角像素配对）
- ε-MSE 全范围照旧（轨迹契约不动）；CTRL（GPU0，油画对照）继续跑完 50K

## 2. 冒烟证据（起跑前实测）

- 两臂 `SMOKE AUDIT PASSED`；峰值显存 23.07/23.06 GiB < 23.5 红线
- 强制低 t 冒烟：`real_p2_ldif=0.0499`、`synth_x0_ldif=0.0479`、
  `discr_adv=2.81` 全部非零；W3-D 正确输出 `real_p1_skipped=1.0`

## 3. 判据（预注册）

- **健康**：零 Traceback；hit 率 ≈30%（t≤300/1000）；D 输出不单边钉死
  （p2/x0_d_real/fake 连续 >0.95/<-0.95 视为饱和警报）；显存 <23.5
- **主判据（10K 中期检查 + 30K 终评）**：
  1. **采样必须形成**：val_novel_full 必须像 CTRL 一样降到 <0.1（wave-1
     的斑点教训——这是"损失装对位置"的硬门槛，不达标即停）
  2. 中期肉眼（用户）：洞区质感相对 CTRL 的油画是否有可见改善、方向对不对
  3. 终评：eval_anchor_compare（FFC vs CTRL vs W3-D vs W3-P，同 24 样例
     seed=42）+ 用户强制三选一裁决
- W3-D vs W3-P：盲区几何稳定性 + 质感，单变量归因

## 4. 决策地图（穷举）

| 结果 | 结论 | 下一步 |
|---|---|---|
| 采样形成 + 质感改善 | 方向成立，四组件闭环 | 胜者续训至 50K/长跑；W+ s2 阶梯 |
| 采样形成 + 质感仍油画 | 剂量不足（0.1 太保守） | 单变量升 w_ldis/w_lfm（仍以梯度监测为界） |
| 采样不形成（val 卡 0.3） | 潜码空间监督也毁场 → 位置理论再修正 | 转训练时 K 步采样侧损失（对象恒真），另预注册 |
| W3-D 几何崩而 W3-P 稳 | pass1 锚承担几何（有条件的 v12 结论回归） | W3-P 为基座，pass1 低频锚混合 |
| W3-P 油画而 W3-D 不油画 | 结构假设在强 pass2 下仍成立 | W3-D 为基座 |

## 5. 产物位置

- 日志 `train_logs/v18w3_{w3d,w3p}.log`；面板
  `experiments/train_inpainting_diffusion/*_v18w3_*_30k/logs/images/val/`
- 终评 `experiments/anchor_compare_v18w3/`
- 事件与决策追加至本文件；wave-1 事故见 PREREG_V18W1_INCIDENTS.md

## 6. 追加决议（2026-09-12 凌晨，用户指令）

- **预算延长**：W3-D / W3-P → **100,000 步**（依据：InvSR 本身 100K、原版
  WarpGAN 300K；用户指令"训久一点"）。save_interval 5000→10000（控盘）。
- **CTRL 同步**：控制臂 50K 自然跑完后由 `scripts/v18_ctrl_continue.sh`
  自动续至 100K —— 终评按**匹配预算**对比，目录名沿用 _50k（历史原因，
  resume 逻辑不受影响）。
- 中期判据不变（采样形成 + 肉眼质感方向）；30K 处的"质感仍油画→升剂量"
  分支依然有效，只是裁决可推迟至 50-100K 之间按用户方便进行。
- 复盘完成的三条数值恒等式（验证实现与设计逐位一致）：
  1. hinge = 0.5(relu(1-real)+relu(1+fake))：real=0.6434/fake=0.6173
     → 0.987 == 日志 0.9870 ✓
  2. W3-D real_total（hit=1）：0.2213+0.0858×1+0.0073×2+0.6152×0.1
     = 0.3832 == 日志 0.3831 ✓（权重 1:2:0.1 实锤）
  3. W3-P real_total（hit=0）：p1 0.0710 + p2 0.0308 = 0.1018 == 日志 ✓；
     synth_total：0.5954+0.0173+0.0012×2+(−0.5386)×0.1=0.5612 == 0.5613 ✓

