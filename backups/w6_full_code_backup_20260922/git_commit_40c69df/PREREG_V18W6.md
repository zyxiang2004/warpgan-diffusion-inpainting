# PREREG_V18W6 — W4 全组装臂（2026-09-13，用户三项裁决后起跑）

## 用户裁决（2026-09-13 中期检查，全部入档）
1. CTRL@50K：沙沙（单像素孔洞）+ 眼眉色块拼接 → anchor 像素噪声可"教出"沙沙
   （修正历史归因）；
2. W3-D@50K：内容跟随渲染（源闭眼/生成睁眼）→ 纯 D 结构泄漏渲染统计，结构
   裁决偏向 W3-P；渲染自身语义失真（反演丢眼态）证实"过度相信 EG3D"警告；
3. W3-P@32K：内容跟随照片证据（闭眼✓）、轮廓最清、但风格"复古油画"且脸偏瘦
   （镜像几何偏差）；
4. SDEdit 阶梯（3身份×4起点）：用户确认 t=300 起点**可见减轻油画感**；
5. prompt 阶梯（8列）：现有空提示权重上 prompt/CFG **无可见差异** → prompt 须
   进入训练（InvSR 即如此）。

## W4 = 三项修正的组装（GPU0，从零，100K，对照=W3-D/W3-P 跑至 100K）
1. `infer.t_start=300`：SDEdit/部分噪声推理成为官方采样（用户确认）；
2. `real_pass1.t_max=300`：**训练域=部署域**（InvSR 范式：其训练时间步
   [100..250]=其推理起点域；高 t 的 pass1 训练教先验生成、推理永不使用）；
3. `prompt.preset=invsr`：训练端恒定正向质量提示词（InvSR 原句；老师②本意
   W+ 与文本特征对齐——空提示为退化态）；
4. `llpips_enable`：真·潜 LPIPS（其主损失件，FM 替身实测数值死亡）。
结构 = W3-P（中期裁决胜方）。

## 验证（冒烟，全绿）
t-domain [0,300] ✓ | CONSTANT prompt ACTIVE ✓ | latent-LPIPS ACTIVE ✓ |
SMOKE AUDIT PASSED ✓ | 峰值 23.11 GiB < 23.5 ✓ | SDEdit 验证采样正常出图 ✓

## 判据
- 主：val 面板（现为 SDEdit 采样）在 10-20K 内洞区质感离开"复古油画"方向；
- 对照：W3-P/W3-D 的 100K checkpoint 也可用 SDEdit 采样重评（推理端修正对
  旧模型同样适用，`eval_anchor_compare` 待扩展 t_start 参数）；
- 若 W4 仍油画 → 剩余疑点收敛到"合成批渲染统计污染"（唯一未动的渲染通路），
  下一变量为 synth 的 x0 参照或比例。

---

## 附：W6-A / W6-B 长跑（2026-09-15 起跑，v19 脚本；与上文 W4 臂无关）

- 动因：W4 被用户裁决无效且被 anchor 封顶后，回到 W3-P 结构做剂量对照长跑（300K）。
- W6-A = W3-P + anchor_median5 + 真LPIPS + **adv×1.0**；W6-B 唯一差异 **adv×0.1**
  （config diff 核对：仅 `x0.w_ldis` 不同）。对照臂 = W3-P 续训至 300K（纯时长效应）。
- 隔离设计：W6-A vs W6-B 隔离对抗剂量；W6-B vs W3-P 隔离 median+LPIPS。
- 中期判定（2026-09-18 全盘复查，详见 PROJECT_STATUS.md §7）：
  - 可见区净化显著（W6 比源照片级洁净，W3-P 反而脏 3-5 倍）；
  - 洞区高频脏度**未收敛**（W6-B 无趋势、W6-A 同样本+固定种子下 2.8~87 振荡）；
  - 无过拟合/发散证据；三臂不停训（用户裁决）。
- 预注册降级路径：若 ~150K 洞区脏度仍无趋势且振荡持续，W6 主收益记为可见区
  净化+目标净化，洞区转推理端手段或提前 checkpoint 终评。
