# 项目说明：WarpGAN 的 Diffusion 改造（新接手者从这里开始）

> 本文档写于 2026-09-09，目的是让新的 agent 不需要任何对话历史，就能理解这个项目
> 在做什么、代码现状如何、三个月来经历了什么。所有路径都是相对路径（以本文件
> 所在目录为根）。文中每个重要论断都标注了代码文件与行号，或数据文件的位置，
> 请不要信任任何没有出处的说法——包括旧文档和本文件。

---

## 1. 项目要解决的问题，以及总体思路

**问题**：给定一张真实人脸照片，以及它的相机参数和深度图（3D 几何已知），生成
同一个人在另一个相机视角下的照片。把照片按几何投影到新视角后，会有一部分区域
没有任何像素覆盖（称之为"空洞"或 hole，例如转头后露出的另一侧脸颊）。**这些空洞
区域在现实中不存在对应的真实照片**——那个角度没有被拍过。这是整个项目所有设计
困难的来源。

**原版方法（本项目要改造的对象）**：原版 WarpGAN（NeurIPS 2025，代码副本在
`reference/orig_WarpGAN-main/`）先用一个 3D GAN（EG3D）把照片编码成潜码并渲染出
新视角的草图，再把照片像素按几何投影（warp）到新视角，最后用一个前馈网络
（FFC-ResNet，代码里叫 inpaintor 或 SVINet）把破碎的投影图修补成完整图像。原版
的问题：输出虽然结构和身份正确，但质感平滑、缺乏真实照片的皮肤纹理——项目内部
称这种现象为"油画感"。

**我们的目标（老师确定的方案，2026-08-23 定稿，原文见
`reference/docs_archive/ORIG_FAITHFUL_PORT_SPEC.md`）**：保留原版的几何流程和训练
数据组织方式，只把 FFC-ResNet 修补器替换成一组 diffusion 组件：冻结的 Stable
Diffusion 1.5 UNet 作为生成主干、BrushNet（一种 ControlNet 变体）提供条件控制、
通过注意力机制从参考图片注入纹理、通过 cross-attention 注入身份潜码（W+ codes）。
其余一切（数据流、损失的位置、监督目标的来源）保持原版语义。

---

## 2. 术语表（先读这一节，后文所有术语在此定义）

| 术语 | 含义 |
|---|---|
| warp / forward_warp | 把图像像素按深度和相机参数投影到另一视角（`utils/warp/Splatting.py`），投影不到的区域形成空洞 |
| hole / 空洞 / 盲区 | warp 后无像素覆盖、需要"无中生有"补出的区域 |
| pass1（第一次前向） | 输入新视角破损图，输出新视角修补结果。**没有真实照片可对照** |
| pass2（第二次前向） | 把 pass1 的条件图再 warp 回源视角做重建。**有真实照片可对照**，是照片监督的落点（沿用原版设计） |
| warp_warp_img | 源照片 warp 到新视角、再 warp 回源视角的结果，即 pass2 的输入条件。老师指定它作为参考特征的输入 |
| x_mirror | 源照片的水平翻转=镜像视角的真实照片，是空洞内容最接近的真实证据 |
| y_hat / y_hat_novel | EG3D 渲染的重建图（源/新视角）：几何可信，质感失真 |
| W+ codes | EG3D 潜码（14×512），携带身份与视角信息 |
| bank（参考特征库） | 参考图过冻结 UNet 后各注意力层存下的特征；去噪时同层检索注入（`models/referencenet/attention_processor.py`，写入见 `_write_bank`） |
| anchor | pass1 的训练目标图：照片证据与渲染按深度一致性连续混合（`_build_novel_view`） |
| ε-MSE | diffusion 标准损失：预测所加噪声，MSE 衡量 |
| x0 预测 | 由加噪图与预测噪声反解的"干净图"估计 |
| FM / 对抗损失 | 特征匹配 / 判别器损失，把输出的统计分布拉向真实照片 |
| 对称重加噪 | 当前版本核心机制，见 §4.3 |
| 手工门 | 项目内部对"人为硬编码阈值"的统称（t<200 门、频段切分等），当前版本已全部移除 |
| "沙沙感" | 用户对主要缺陷的描述：空洞区出现细密高频噪点，量够但不是此人真实纹理 |

---

## 3. 目录与关键文件

```
WarpGAN-main/                                 ← 本目录（改造均为新增文件，与原版共有文件零差异）
├── PROJECT_STATUS.md                         ← 本文件
├── training/coach_inpainting_diffusion.py    ← 训练主逻辑（约 2200 行，模块头有架构注释）
├── models/referencenet/attention_processor.py← 注意力注入（纹理检索 + W+ 身份注入）
├── models/mapper/w_proj.py                   ← W+ [14,512]→[18,768] token 投影
├── models/wplusnet.py                        ← EG3D 反演（W+ codes、渲染、深度）
├── datasets/dataset_inpainting_static.py     ← 真实照片批数据
├── datasets/dataset_inpainting_synth_static.py ← 合成配对批数据（EG3D 双视角）
├── utils/warp/Splatting.py                   ← 前向投影（原版原样）
├── utils/warp/splatting_ext.py               ← 反向投影 + 连续置信度场（新增）
├── configs/train_inpainting_diffusion.yaml   ← 全部开关（当前配方段：losses.x0）
├── scripts/train_inpainting_diffusion.py     ← 训练入口（hydra）
├── scripts/eval_anchor_compare.py            ← 三方基准对比评测（原版 FFC vs 两个 diffusion 模型）
├── scripts/v17prime_fresh.sh, v17prime_watchdog.sh ← 当前配方的启动与守护脚本
├── tests/                                    ← 历代配方的守护测试
├── reference/orig_WarpGAN-main/              ← 原版代码完整副本（diff 基准，只读）
├── reference/models_lib/                     ← 创新模块来源库（BrushNet 源码等）
├── reference/docs_archive/                   ← 全部旧文档（考古用，可靠性见 §5）
├── data -> （符号链接，指向数据集，迁移后需重建）
├── pretrained_models -> （符号链接，指向权重目录，迁移后需重建）
└── experiments/                              ← 历代实验产物；experiments/anchor_compare/ 存放基准对比数据
```

---

## 4. 当前代码的架构（代码里实际存在的形态）

### 4.1 模块组成（`training/coach_inpainting_diffusion.py`，`Coach.__init__`）

**冻结不训练的部分**：Stable Diffusion 1.5 的 VAE 和 UNet、空文本提示的 CLIP 编码、
GOAE/EG3D 反演网络（只在启用 latent 闭环损失时加载）、两个 warp 工具。VAE 和 UNet
设为 train 模式并开启梯度检查点——这不是为了训练它们，而是让梯度能穿过它们回传
到 BrushNet，同时省显存（网络本身无 BatchNorm 统计问题，train 模式数值不变）。

**参与训练的部分**：BrushNet（用官方 inpainting 预训练权重初始化，是唯一的大模块）；
安装在 SD UNet 每个注意力层上的 `ReferenceAttentionProcessor`（其中纹理检索的
K/V/O 适配器约 37M 参数、mirror 独立 K/V 约 25M 参数）；可选的判别器（NLayer
PatchGAN，约 7M 参数，当前配方启用）。W+ 的投影层（`wplusnet`→`w_proj`）使用早期
预训练的权重并冻结（开关 `wplus.mode: frozen`，其他档位见 §6）。

### 4.2 一次训练迭代的数据流（真实批与合成批 1:1 交替，各自独立做优化器步）

**真实照片批，第一次前向（pass1，新视角）**：
输入条件 = warp 可见区 + EG3D 新视角渲染填洞的混合图（条件端的掩码做了腐蚀+模糊
软化，这是原版的处理方式）；参考特征库的主源是镜像照片 x_mirror、副源是新视角
渲染；损失 = 对 anchor（§2 术语表）做单一全带的 ε-MSE。

**真实照片批，第二次前向（pass2，回源视角）**：
把 pass1 的条件图再 warp 回源视角作为输入（即 warp_warp_img），用源视角渲染填洞；
参考特征库的主源是 warp_warp_img（老师指定的），副源是源视角渲染；损失 = 对**真实
照片 x** 的全帧 ε-MSE，加上下述 x0 分布监督。这一步的含义是：新视角没有真值，
但把任务转回源视角后就有真值了，网络在源视角学到的"把破损图修成真实照片"的
能力，会迁移到新视角的使用中（原版正是这样设计的）。

**合成批**：
数据是 EG3D 对同一虚拟身份渲染的双视角精确配对（输入视角图 + 目标视角图都有）。
ε-MSE 全带地对配对渲染；x0 分布监督的"真实"参照用的是该身份的**源照片**
（合成数据的源图也是 EG3D 渲染的，但统计上更接近照片分布；这是有意的选择，
理由见 §5.3 证据分级）。W+ 身份分支只允许在这一批更新（真实批上会积累错误
梯度，这是历史教训，见 §6 红线）。

### 4.3 x0 对称重加噪分布监督（`Coach._x0_losses`，当前版本的核心新增）

**要解决的问题**：ε-MSE 是回归损失，它能教会网络"结构长什么样"和"高频能量有多
少"，但教不会"这块区域此刻应该是什么质感的纹理"——这种知识只能来自真实照片的
统计分布，也就是需要判别器类损失。历史上（v14 及更早）把这类损失直接加在 x0
预测图上会训练崩溃，原因有二：一是参照对视角不匹配，判别器会先学会用"视角对
不对"这种与目标无关的线索区分真假，然后给生成器的梯度就全错了；二是 x0 预测
图在高噪声 timestep 上含大量残噪，判别器同样会走"看噪声水平"的捷径。

**机制**（无任何人手设定的阈值）：
```
x0_hat = (x_t − √(1−ᾱ_t)·ε_pred) / √ᾱ_t        ← 从噪声预测反解干净图
fake_t = √ᾱ_t·x0_hat + √(1−ᾱ_t)·ε_new          ← 把干净图重新加噪到与 x_t 相同水平
real_t = x_t（真实批）/ 同样方式加噪的照片（合成批）
```
判别器与特征匹配损失比较的是 (fake_t, real_t) 这一对。因为两边噪声水平相同，
判别器无法用噪声区分真假，只能看内容与质感；当 timestep 趋近最大值时，两边都
退化为纯噪声，监督自动失效（不再需要"只在 t<200 施加"这种人为限制）；梯度经过
fake_t 的构造式自动带上 √ᾱ_t 因子（等效于信噪比加权，无需手写权重）。像素 L1
损失的权重也取解析值 ᾱ_t。

**被移除的历史机制**（代码路径保留、由 `losses.x0.enable` 开关旁路，可回退对照）：
t<200 timestep 门、x0 截断值 3.0（现放宽为 8.0 仅作数值保护）、按 σ=2 潜空间像素
划分结构/纹理频段的监督方式、双频带加权（dual-band）及其 13 像素过渡带、随机
patch 解码与 L1×2 权重。移除理由：它们都是分布监督缺席时期的替代品，用户指出
这些"手工门"不符合第一性原理；分布监督回归后由其取代。

---

## 5. 项目历史（三个多月，四十多个版本）

历史分三个阶段。前两个阶段的详细记录在 `reference/docs_archive/`（其中
`PROJECT_HISTORY.md` 是早期历史，`STEP0_SMOKE_REPORT.md` 是移植后的逐轮记录，
`README.md` 有版本一览表）。**注意：旧文档中至少有两处根因分析后来被推翻**
（§8.44 与 §8.47 的结论均被后续勘误修正），所以旧文档只能当事实记录看，
其中的分析结论要以代码和对照数据为准。

### 5.1 第一阶段：早期自研（2026-05 ~ 08-23）

从零搭建自己的 diffusion 方案，尝试了联合训练、双分支结构、各种频段监督、可训练
W+ 分支等大量方向，四个月未达到可接受的效果。这一阶段的价值是沉淀了 22 条
被证伪的方向清单（`PROJECT_HISTORY.md` §4）——后续所有版本都靠这份清单避免
重复踩坑。

### 5.2 第二阶段：忠实移植与逐版本迭代（08-23 ~ 09-08）

按老师的五句话规格把原版翻译成 BrushNet + SD1.5（代码结构就是现在这份），然后
以"每次只改一个变量"的方式迭代了约 16 个版本：

| 版本 | 改动 | 结果 |
|---|---|---|
| step1b | 纯 ε-MSE 基线（25K 步） | 成为后续所有版本的对照基线 |
| v1-v3 | 加入镜像照片参考 | 整代无效——续训时命令行参数覆盖丢失的事故 |
| v4-v7a | 镜像监督方式的一系列修正 | 修复了大量实现问题，效果趋于稳定 |
| v8 | 删除独立的参考网络，改用主 UNet 自身存特征 | 与原方案像素级等价，省下 860M 参数 |
| v9 | 合成批只监督结构频段 + 镜像照片作为主参考源 | 微小改善 |
| v10 | 条件端掩码软化（原版语义） | 负结果：证明条件端不是接缝问题的瓶颈 |
| v11 | 空洞零监督 + 局部检索窗口 | 失败：盲区高频漂移被实测 |
| v12 | 盲区低频锚定（dual-band） | 用户裁决为当时最好的结构，但"沙沙感"未解决 |
| v13 | 恢复原版全部损失项 | 失败：v12 优于 v13 |
| v14 | 加入 GAN/FM（参照对视角错位） | 训练崩溃（判别器走了视角捷径） |
| v15 | 盲区高频保持约束 | 失败 |
| v16 | pass2 像素回流（t<200 门控的 L1） | 与 v12 指标无差异（本次对话核实） |

这个阶段的普遍模式：每一版训练约 17-24 小时，裁决依赖肉眼观察和不敏感的 L1
指标，而且**从未与原版输出做过对照**。

### 5.3 第三阶段：诊断转折（09-08 ~ 09-09，产生当前代码）

1. **第一次与原版做基准对比**。按老师要求"可视化要对比原版结果、多看不同
   case"，写了 `scripts/eval_anchor_compare.py`：原版 FFC 模型（权重
   `pretrained_models/inpaintor.pt`）与 v16、v12 在同一批 24 个测试样例上
   （8 个身份 × 3 视角，空洞占比 0.03 到 0.68）用相同随机种子生成并对比。
   第一次运行时 FFC 的推理复现有 bug（混合条件图的可见区误用了原图而不是
   warp 后的图），**用户看图发现"人脸是原来角度的"而定位了这个问题**，修复后
   重跑。数据在 `experiments/anchor_compare/`。

2. **对比结果**（`anchor_metrics.txt`）：原版在像素保真上全面领先（空洞区 L1
   0.074 vs 我方 0.087/0.089）；原版空洞区高频能量只有真实照片的 36%，但用户
   终审确认它的质感是"内容自适应"的——该模糊的地方（虚化的背景）就生成模糊，
   轮廓自然；我方高频能量与照片相当，但用户三轮肉眼终审确认内容是错的
   （"沙沙感"），且高频超标的样例与沙沙位置吻合。用户还确认了一个此前不知道
   的问题：训练目标图 anchor 本身在空洞大时是破碎的（有拼接痕迹、五官错位）。

3. **根因结论**（四条相互独立的证据：原版损失代码的行号、我方配置、对比数据、
   用户目击）：原版的质感来自 pass2 位置上每步全帧的分布监督（对抗×10 + 特征
   匹配×100，参照物是真实照片，见 `reference/orig_WarpGAN-main/training/
   coach_inpainting_static.py` L1118-1196）。diffusion 版把这一族损失整体关闭后，
   只剩 ε-MSE——它教得出结构和能量，教不出"什么内容配什么质感"，于是空洞区的
   高频来自 SD 预训练先验的平均统计（这就是"沙沙"的本质）。

4. **当前代码（v17-prime）**：按用户指令"拒绝手工门、适配要符合第一性原理"
   实现 §4.3 的机制。冒烟测试全部通过（特征匹配损失触发、判别器步运行、W+
   梯度为零、显存峰值 21.28 GiB 低于 22 GiB 预算、ᾱ_t 解析权重与实测一致）。

---

## 6. 用户的角色、历次关键质疑，与技术红线

### 6.1 分工与质量防线

方向决策、方法论原则、每个版本的最终裁决（肉眼终审）由**用户**做出；历任 agent
负责读代码、写实现、跑实验、做数据分析。历史证明这道防线不可省略：agent 的
根因分析出过错（旧文档两处被推翻），agent 的实现出过错（基准对比的 FFC bug，
由用户看图发现）。因此对新 agent 的要求：**每个论断给出处；分析必须有对照数据
支撑，只有推理的结论要标注为假设；用户的肉眼终审优先于自动指标；启停训练、
修改配方这类重大动作先请示后执行。**

### 6.2 用户历次关键质疑（都产生了实际后果）

| 用户的质疑/指令 | 结果 |
|---|---|
| 不信任先前的记录 | 合理：旧文档确有两处根因分析被推翻 |
| 怀疑 agent 没有真正读完代码 | 合理：上一任 agent 因未读损失段得出过错误结论；此后两个训练主文件已 100% 通读 |
| "四十多轮都按建议走，怎么跳出循环" | 诊断成立：历轮从未与原版对照、指标无法区分版本——直接催生了基准对比 |
| "t<200 这种感觉怪怪的，要第一性原理" | 成立：清点出一整族硬编码阈值，全部是历史事故的产物，已在当前版本移除 |
| "可能你的代码写错了，人脸是原来角度的" | 属实：抓住了 FFC 复现的 bug |
| "GAN/FM 炸过，是不是实现有问题" | 复盘结论：两次失败都是实现/位置错误，不是这类损失不适配 diffusion |

### 6.3 技术红线（历代实证，违反即重蹈覆辙）

1. 像素/感知损失不能直接作为 ε 的训练目标（历史上两次训练爆炸）——只能走
   x0 对称重加噪路径；
2. 判别器/特征匹配的参照对必须同视角、同噪声水平（v14 因视角错位崩溃）；
3. 判别器的真实样本永远用照片，不用渲染图（否则质感被拉向"塑料"）；
4. W+ 分支只在合成批更新（早期 §17.8 教训）；
5. 训练 timestep 保持全范围 [0,1000)（只训低噪声段会导致输出雾化）；
6. 输出整帧生成，不做硬拼接；续训必须重申全部命令行覆盖并核对生效配置
   （v1-v3 整代事故）；
7. 二值掩码不进监督（产生碎片伪影）；空洞不能长期零监督（高频漂移）。

### 6.4 尚未验证的遗留项（代码已留接口）

- W+ 阶梯：老师方案的第二步"训练对齐投影层"（`wplus.mode: s1_mapper`）从未执行，
  一直用冻结档；
- anchor 的破碎问题：空洞大时镜像证据破碎仍进入监督目标（用户目击，§5.3-2）；
- "pass2 学到的能力能迁移到 pass1"这一原版核心假设，未经专门对照检验。

---

## 7. 2026-09-18 状态快照（三臂长跑中期全盘复查，用户裁决入档）

> 本节由当日 agent 全盘检查后写入。此前各节写于 09-09。所有数字可由 §7.6 命令复现。

### 7.1 在跑实验（均零 Traceback，~1.05-1.10K 步/h，显存 18-20G，GPU 0/1/2）

| 臂 | GPU | 配方 | 当日进度 | 预计完成 |
|---|---|---|---|---|
| W3-P 续训 (`v18w3_w3p`) | 2 | W3-P 原方（无 median、无真 LPIPS、adv×0.1） | ~170K/300K | ~9/23-24 |
| W6-A (`v19_w6a`) | 0 | W3-P + anchor_median5 + 真LPIPS + **adv×1.0** | ~80K/300K | ~9/26-27 |
| W6-B (`v19_w6b`) | 1 | 同 W6-A，唯一差异 **adv×0.1**（config diff 已核对：仅 `x0.w_ldis` 一行不同） | ~82K/300K | ~9/26-27 |

W6 双臂配置要点（`[20260915-085452]_v19_w6a_300k/config.yaml`）：`x0.mode=invsr,
t_max=300, w_ldif=1.0, w_lfm=2.0, dis_warmup=3000, llpips_enable=true,
d_real=paired_target`。

### 7.2 方法论更正（影响所有旧 val 数字的解读）

1. **val 批次在 3 个固定样本间轮换**（洞区像素数仅取 112170/166416/204907 三个
   离散值，采样种子固定 42）。此前把 val 波动当作训练动态是错的——主体是样本
   难度差异。**跨步、跨臂比较 val 必须按样本分组**（本节所有结论均已分组）。
2. 训练损失（ε-MSE 族）从 ~4K 步起就平（diffusion 条件熵底），对收敛判断无效。

### 7.3 收敛判定（按样本拆分 val_novel_hole）

- W3-P 三样本均在 **35-70K 步到达 L1 平台**，此后 100K+ 步无下降：
  112170: 0.15→0.08@67K 后平；166416: 0.11→0.055@61K 后缓磨至 0.045；
  204907: 0.14→0.05@35K 后平。
- W6-A/B 已达各自平台（204907 样本 0.033-0.05，与 W3-P 终值持平）。
- **判定：保真度收敛已完成且正常；质量（质感）演化对 L1 不可见**（历史先例：
  W3-P 67K→94K 期间 L1 全平但用户肉眼裁决明显变好）。

### 7.4 "背景和纹理不干净"专项定量（用户 2026-09-18 感受触发）

同样本+近似同步数三臂对比。speck3 = 3×3 中值偏离>20 的孤立斑点/千px；
blotch7 = 7×7 同法（较大尺度脏块）；vis = 可见区（非洞）。

| 样本 | 臂 | 洞speck3 | 洞blotch7 | 锚点blotch7 | 洞纹理 | 可见区speck |
|---|---|---|---|---|---|---|
| 204907大洞 | W3P@83K | 44.3 | 84.7 | 41.6 | 8.7 | 14.8 |
| 204907大洞 | W6A@79K | **2.8** | **12.0** | 2.9 | 3.0 | **0.5** |
| 204907大洞 | W6B@79K | 75.7 | 125.9 | 2.9 | 12.4 | 1.7 |
| 166416中洞 | W3P@79K | 43.7 | 86.9 | 82.9 | 8.4 | 39.3 |
| 166416中洞 | W6A@67K | 31.4 | 89.6 | 6.9 | 8.9 | 5.6 |
| 166416中洞 | W6B@67K | 47.6 | 111.2 | 6.9 | 10.5 | 10.9 |
| 112170小洞 | W3P@85K | 45.8 | 122.0 | 134.6 | 10.2 | 26.7 |
| 112170小洞 | W6A@75K | 18.9 | 74.3 | 12.4 | 8.4 | 5.0 |
| 112170小洞 | W6B@75K | 8.2 | 56.2 | 12.4 | 6.7 | 5.5 |

（参照：源照片可见区 speck = 2.9/7.0/8.7；渲染 y_hat_novel = 0.0-0.2）

**五条发现：**
1. **可见区（真背景）W6 比 W3-P 干净 3-10 倍，且达到源照片自身洁净水平**；
   W3-P 可见区反而比源照片脏 3-5 倍。"背景不干净"若指画面可见区，与定量相反；
2. **洞区脏度：W6-B 与 W3-P 相当（到 80K 无改善）；W6-A 在 2.8~87 间振荡**。
   用户感受与洞区指标一致——脏集中在洞区（头发/耳后/下颌等生成内容）；
3. 锚点已被 median5 洗净（blotch7 2.9-12.4 vs W3-P 41.6-134.6）→ **洞区脏不是
   从目标学来的，是生成/采样端自发的高频噪声，集中于上下文最稀缺的大洞区**；
4. **同样本+固定种子下，相邻 val（2K 步间距）洁净度大幅翻转**（W6A 的 204907：
   78.8@56K→3.8@58K→66.6@60K→68.5@64K→32.3@74K→87.4@78K→2.8@80K；W6-B 在
   58K 同步塌落到 ~3 后恢复）→ 高频行为处于"干净模式/脏模式"不稳定平衡，
   不是平滑收敛中的样子；
5. 对抗分量健康但量小：gen_adv -0.05~-0.35、discr_adv 1.0-1.3、梯度有界无发散；
   W6-A 的 d_grad(~20) 比 W6-B(~350-520) 低一个量级（w_ldis 副效应，均在界内）。

### 7.5 裁决与待办

- **判定**：保真收敛正常；可见区 W6 显著优于 W3-P（median/LPIPS/adv 组合的净收益）；
  **洞区高频脏度未收敛**（W6-B 无趋势、W6-A 振荡）。不是过拟合（同样本脏度不随
  步数单调升、train-val 无背离、58K 双臂同步自愈），不是发散。
- 三臂维持不停训（用户 2026-09-18 裁决）。
- 待办：
  1. W6@100K 同样本视觉复查（预计 09-18 晚到达）；
  2. **W6-A"干净模式"种子敏感性检验**：同一 checkpoint（如 80K）多种子采样，
     判定干净态是权重态还是采样态——若多种子均干净则可采样端锁定；需 GPU，
     排 W3-P 完成后（9/23 释放 GPU2）或低峰期执行；
  3. 若 ~150K 洞区脏度仍无下降趋势且振荡持续 → 降级路径：W6 主收益记为
     可见区净化+目标净化，洞区脏度转推理端手段（多种子挑选/轻度引导），或提前
     checkpoint 终评（每 10K 有存档）。

### 7.6 复现命令（要点）

```bash
# 按样本分组的 val L1：解析 train_logs/*.log 的 [val step] 行 + 面板第4格(mask)暗像素计数
# 斑点/斑块/可见区指标：val 面板 6 格切分 [x|render|cond|mask|anchor|gen]，
#   speck3/blotch7 = |img - MedianFilter(k)| > 20 计数/千px，PIL ImageFilter 实现
# 配置差异：diff experiments/train_inpainting_diffusion/*w6a*/config.yaml *w6b*/config.yaml
# 损失分桶：train_logs/v19_w6{a,b}.log 按 4K 步分桶均值（gen_adv/discr_adv/lfm/grads）
```

### 7.7 洞区脏度根因分析（2026-09-18 晚，对照 gpt.md 与代码核实）

**三层根因（全部有代码/数据出处）：**
1. **监督目标分布错位**（gpt.md §4/§6 的原话级预测）：洞区全部参照损失
   （ε-MSE 全 t、ldif、LPIPS）都以 anchor 为目标（`_x0_losses_invsr` 的
   `tgt = out['target_latents']`），anchor 洞区统计 ≠ 照片统计。实测吻合：
   可见区（照片证据监督）W6 达源照片级洁净，洞区（anchor 监督）持续脏——
   "脏"即 anchor 洞区统计与照片统计的残差。
2. **照片参照的对抗损失没有打到洞区**：现有 D 配对仅 `real_p2`（real=照片，
   但作用在 pass2 重建，对 pass1 洞区的转移依赖"pass2 迁移"这一未验证假设）
   和 `synth_x0`（**real=成对渲染 latent，违反红线 #3**，代码注释
   L1298-1299 自证）。**没有任何 D 配对看到 pass1 新视角洞区内容**——部署
   路径上洞区纹理从未被照片统计约束。这解释 adv×0.1 vs ×1.0 对洞区脏度无差：
   打的不是那个靶。
3. **高频相位维度无约束→随数据课程漂移→模式振荡**：ε-MSE/L1/LPIPS 对高频
   相位不敏感；每批 mirror 覆盖率波动大（real_p1_mirror_hole_cov 0.004-0.34）；
   无约束维度跟课程漂移，两臂共享数据顺序故同步（58K 双塌落）。

**修正方向（gpt.md §11-12 处方，未执行的部分）——W7 提案（待用户裁决）：**
- pass1 洞区 masked patch D：fake = pass1 x0_hat 洞区 crop（t≤300），real =
  真实人脸照片 patch（gpt.md：D 不需要洞区 GT）。UNet-D/D 队列/pass1 前向均
  现成，改动集中在 `_disc_queue_invsr` 入队与 crop 采样；
- 同时修 `synth_x0` pair 的 real：渲染→照片（修红线 #3 违规）；
- 风险预注册：严格保持 t≤300（W1 教训：全 t 分布监督毁采样）；D 预热沿用；
  剂量从 ×0.1 起步；
- 时机：W3-P 完成（~9/23 释放 GPU2）后 20-30K 冒烟，或等三臂长跑全部出结论。
- 次要参考：DMDX（若 hinge 加对靶后仍振荡，换对抗分布匹配）；ReSem-Face
  解决的是身份保持而非洞区质感（W+/参考注意力已覆盖其角色）；AnyDoor HF 已
  验证无效（W5）。

### 7.9 "好/坏振荡"的再标定（2026-09-18 淚点：用户标注样本 81999 入档）

用户点名 `val_step0081999.png`（W6-A@82K，样本 166416）为"确定就是想要的
结果"。定量核实：该面板 speck3=53.2、blotch7=137.1、tex=11.63——**在我此
前的"噪声指标"里属于脏端**。结论修正：

1. **模型在两种失败模式间摆动，不是好↔坏**：
   - "丰富但噪"端（tex 11-12，speck 50-60）——@82K 属于此端，组织得好时
     被用户认可；
   - "干净但涂抹"端（tex 3.7，speck 6.5，@42K 极端）——此前被我误标为
     "干净模式"，对用户可能恰是糊/涂抹（与 anchor_median5"涂抹代价"裁决
     一致，待用户标注确认）；
   - **用户偏好位于丰富纹理端**——"低 speck=好"的解读作废。
2. 振荡与训练窗口粗统计（洞占比/镜像覆盖/p2_t/D_adv，前 2K 步均值）**无
   相关**（204907 全序列核实）→ 驱动是权重在高频能量维度上的自由漂移
   （§7.7 层3），50 步采样器对该维度是放大器（能量微增→发丝/沙沙，微减→
   涂抹）。
3. L1 对用户偏好同样盲（@82K L1=0.096 高于 @84K 0.068，但 82K 被认可）。
4. 行动：①把用户标注变成数据（标 8-12 张面板好/坏→拟合判别指标）；
   ②checkpoint 收割：80K checkpoint 在盘上且同期 val 为最佳之一；
   ③结构性修复仍是 W7 洞区照片 patch D（把高频能量锁到照片统计），
   EMA 权重为备选稳态手段。

### 7.10 用户校准结果 + 两臂同步性检验（2026-09-18 晚）

**校准（user_calibration_20260918/，172 面板全量打分）**：
- 规则 GOOD=tex≥9（tierA: speck3≤60）、SMEAR=tex≤5 对照；GOOD 82 张
  （w6a 15 / w6b 24 / w3p 43）、SMEAR 15 张。
- 用户裁决：**w6a/w6b 筛选基本符合预期，两臂效果差不多** →
  tex≥9+tier 标准在 W6 域内有效；adv×10 vs ×0.1 再次无效。
- w3p：仅高步数部分达到预期（带细碎小孔），低步数 GOOD 面板呈油画感 →
  tex 测"有没有纹理"而非"纹理真不真"，跨域需加成熟度/域门控。
- 好图的"细碎小孔" = tierA 残留 speck（30-60），差图的"孔洞" = 低增益下
  结构缺失（平坦色块与周围皮肤统计断崖）——同名不同源。

**两臂同步性检验（/tmp/calib_sync.py，相同样本相同步数配对）**：
- 两臂脚本 diff 仅 w_ldis 与 GPU → 同种子同数据顺序。
- tex 序列 Pearson r=0.30/0.22/0.18（三样本），GOOD 判定一致率
  86%/53%/54% ≈ 抛硬币 → **精细翻转不同步**。
- 反例窗口 39-47K（样本 204907）：w6a 涂抹端(4.0-4.9) 而 w6b 丰富端
  (11.3-13.0)——同数据同步骤相反模式；57K 处两臂同跌 3.3/3.3（58K 事件）
  → 偶发共享强事件（课程切换）仍存在。
- **结论：快翻转主导驱动=各臂自身 SGD 噪声在无受力维度上的随机游走**，
  非数据课程的确定性函数；课程仅偶发重击。

### 7.11 参照解法勘察（2026-09-18 深夜，W7 实施前的文献/代码考古）

用户要求：先在 refnet/BrushNet/后续参照项目中找"存在参照的解法"再动手。
勘察结论按证据强度排序：

**① 原版 WarpGAN（最强：代码在库、组件在我们管线里每步都在算）**
- `training/coach_inpainting_static.py` L415-434（D 更新三分支）：
  real **全部=真实照片 x**；fake=pred_novel（新视角补全，无配对分布监督）
  + **pred_inv_warp（新视角输出经 inverse_warp 回原视角，mask_inv，与 x
  成对——同视角/同身份/同区域的照片监督）** + pred_inv_pred 分支；
- L529-536 synth 分支：rec 的 real=**target_img 真实照片**（非渲染！），
  L559 synth D 更新同样 real=照片；
- 损失类 NonSaturatingWithR1 带 mask 聚焦机制（mask_as_fake_target/
  extra_mask_weight_for_gen，saicinpainting "洞区即假"设计）；
- 剂量：adv ×10 + FM ×100（原版洞区质感即靠此撑起）；
- **我们的 diffusion coach L1566-1582 每步都在算 `warper_ext.inverse_warp`**
  （现仅用于拼 mirror anchor）——原版喂 D 的算子现成，从未喂过 D。
- 移植时 D 配对全部重指向（synth_x0 real=渲染、real_p2 只打 pass2），
  pass1 洞区照片监督自此消失 = §7.7 层2 缺口的由来。

**② InvSR（架构与哲学参照，已在库）**：fake=模型实际输出量（x̂0）、
real=GT 照片 latent；**且 InvSR 用 EMA（ema_rate）**——为我们 EMA 阻尼
选项提供同领域参照。

**③ BrushNet/SD-inpainting（概念参照）**：训练损失本身=洞区照片 GT 的
ε-MSE；pass1 新视角无 GT 不可直接用，但指明方向——**造出有照片 GT 的
监督路径**，即 ① 的 inv-warp 回 warp。

**④ AnyDoor（弱参照，勘误）**：官方放出训练代码**无判别器**（
run_train_anydoor.py 仅 ControlNet 式扩散损失），patch D 仅存在于论文；
其真正的纹理方案（DINOv2 HF）我们 W5 已试无效。

**⑤ DMDX（备用）**：分布级对抗匹配（ADM），若成对 D 后仍振荡再上。

**W7 据此修订（从"发明"降级为"复原+适配"，三段式）**：
- ~~W7a~~ / ~~W7b~~ / ~~W7c~~ → 实施时再收敛，见 §7.12/§7.13。
- **§7.11 勘误（2026-09-18 实施时发现）**：原版 synth 分支 D 的 real 是
  `src_img`（**渲染**，static coach L554-558），不是照片——盘上
  SynthData100000 四个 PNG 哈希各异且 config L112 注明"synthetic
  identities have no real photo"。因此我们的 synth pair（real=渲染）本就
  与原版一致，**W7c（改 synth real）撤销**——改它才是无参照的臆造。
  原版 D 的照片 real 只存在于 real-batch 分支（novel/inv_warp），修复
  范围收敛为 W7b 单点。

### 7.12 W7a/b 争点收敛（2026-09-18）：c 非路线、b 定主、a 由预实验门控

用户质询"为什么要分 abc、现在不能判最优吗"——澄清与判定：
1. **W7c 不参与比较**：它是红线 #3 违规的 bug fix（synth_x0 real 渲染→
   照片），无条件执行。
2. **a vs b 的唯一争点**：成对信息量 vs warp 算子对高频的破坏。
   - a 的优势：成对（同身份/区域/光照）、原版存在证明；
   - a 的两个量化弱点：① 双线性重采样 + 流对齐误差 ~1-3px ≈ 毛孔/
     发丝波长（2-4px）→ 高频尺度上"成对"名存实亡，D 会学"warp 指纹"
     而非真实感（梯度指错方向的经典 GAN 病理）；② **占空比实测**
     （v19_w6a.log 1701 步）：mirror_hole_cov 中位 0.080、>0.10 仅 42%、
     >0.20 仅 16% → 成对监督面积小且间歇。
   - b 的优势：fake=部署量本身零混淆；洞区全覆盖恒定；gpt.md §11-12
     处方；InvSR 哲学（fake=模型实际输出量）；诊断出的自由维度（高频
     统计漂移）用分布级恢复力即可约束，不需要成对。
   - b 的弱点（无身份约束）不构成问题：低频结构/内容由 ε-MSE+anchor
     已管，不是无受力维度。
3. **预注册决策**：W7b 为主路径立即实现；W7a 门控于 30 分钟离线预实验
   （真实照片过 inverse_warp → 量高频衰减 + 用现有 84K D 看 logits
   能否区分"warp 过的真照片 vs 真照片"；可分 → warp 指纹风险实锤，
   a 弃；不可分 → a 作为 b 的补充分支，原版本就双分支）。
   队列基础设施 a/b 共用，代码代价近零；训练只跑单主路径，不做训练期
   A/B。

### 7.13 W7 实施（2026-09-18，用户指令：停 W3-P、备份、基于原版修复、续训）

用户决策链：停 W3-P（释放 GPU2，终值 174950 步，170K ckpt 在盘）→
基线提交 `40c69df` + tag `v19-w6-baseline` → W7b 实施 → W6-B@80K 续训。

**基底选择 W6-B（自行决定，理由）**：① 用户校准两臂效果差不多，但
tex≥9 GOOD 数 24 vs 15、大洞样本 tex 驻留 12-13 vs 11-12，W6-B 略优；
② adv×0.1 = InvSR 校准剂量，叠加新 D 信号时总对抗压力不超参照；
③ W6-B 仍在 GPU1 运行 → 同血统直接对照（80K 分叉点起 W7 vs W6-B）。

**实施（全部有参照，零臆造）**：
- `configs/train_inpainting_diffusion.yaml` losses.x0.hole_d
  {enable:False 默认（保护 W6 双臂重启路径）, weight:0.1, warmup:2000}；
- coach `__init__` L442-452 读选项（缺省全关）+ pass1 块 L1803-1853：
  fake = pass1 x̂0 × 软洞掩码（13px box→area，v12 配方），real = 同一
  掩码 × 照片 latent（.mode() 编码，与 target latent 同约定）；两侧掩码
  相同 → D 只能靠洞区纹理统计判别；G 侧仅 adv（无 ldif/llpips——新视角
  vs 源视角禁止回归靶），D-only 预热 2000 步（从首个配对起算，resume
  感知）；t≤300 沿用；synth pair 原样保留（即原版行为）。
- `scripts/v19_w7_holed.sh`（GPU2，W6-B 全配方 + hole_d 三覆盖）；
- ckpt 拷贝续训：W6-B iteration_0079999.pt（5.57GB）→
  `v19_w7_holed_300k/checkpoints/`（避免与运行中 W6-B 同目录冲突），
  附 config_w6b_origin.yaml 出处。
- 判据（预注册）：烟雾=[W7] 打印 + holeD_hit=1 + holeD_d_real/fake 非
  零 + 0 Traceback + 显存<23.5GB；疗效=同样本 val 序列 tex 振荡幅度
  收窄（对照 W6-B 同期），speck 向 tier-A 收敛，洞区 blotch 下降。

### 7.14 W7 早期疗效读数（2026-09-19 晨，@99.5K；G-adv 自 ~82.1K 激活）

健康：三臂全绿（W6-A@105K / W6-B@108K / W7@99.5K，GPU 95-100%，
W7 无新 Traceback，gen_adv 均值 +0.32 有界，D 分离 real≈+0.9/fake≈-1.0
健康）。同步数同样本对比（82K-98K，9 个 val）：

| 指标 | W7 | W6-B |
|---|---|---|
| tex 均值 | **6.28** | 5.07 |
| SMEAR(tex≤5) 次数 | **4/9** | 7/9 |
| GOOD(tex≥9) 次数 | 2/9 | 2/9 |
| tex 标准差 | 3.78 | 3.19 |
| sp3 最大 | **108.7**(@86K) | 69.7 |

读数（诚实版）：
1. **方向正确**：照片 D 把洞区从涂抹端往上推（W6-B 同窗 7/9 涂抹，
   W7 逃逸 5/9；tex 均值 +1.2）——恢复力假设的首个定量支持。
2. **尚未收敛**：振荡幅度未收窄（std 反而 3.78>3.19，由 @86K 过冲
   驱动：tex15.5/sp3=109/bl7=138，进入"噪端极端"）。
3. 备择假设在案：@86K 过冲可能是 D 用视角差判别（§7.13 预注册风险），
   G 以视图不变噪声满足 D；也可能是正常早期振荡+均衡点上移。
   指标无法分辨，需用户肉眼对比 W7 vs W6-B 同步面板（尤其 @86K/88K）。
4. 剂量调整预案（不立即执行）：若 ~110K 前再出现 sp3>100 事件≥2 次，
   hole_d.weight 0.1→0.05；否则维持到 ≥120K 再判。
5. 判定时点：≥120K（≈40K 步 hole-D 训练）做正式疗效裁决，勿提前。

### 7.15 W7 @123K 扩展读数与预注册裁决（2026-09-19）

21 个同步 val（82K-122K，/tmp/w7_efficacy.py 同脚本）：

| 指标 | W7 | W6-B |
|---|---|---|
| tex 均值 / std | 6.96 / 3.79 | 6.26 / 3.76 |
| GOOD(tex≥9) | 6/21 | 7/21 |
| SMEAR(tex≤5) | **10/21** | 13/21 |
| sp3>100 事件 | 1（仅 86K） | 0 |

**分样本模式（关键）**：中洞 166416 W7 明显更常逃出涂抹（118K:
10.9 vs 3.7；88K: 9.8 vs 3.5）——主要获益样本；大洞 204907 两臂几乎
相同（双丰富帧 W7 tex 略高 +1 左右）；小洞 112170 反转（W6-B 更常丰富，
100K: 4.8 vs 11.2）。

**裁决（按 §7.13 预注册判据"tex 振荡幅度收窄"）**：未达成——std
3.79 vs 3.76 持平；达成的部分：涂抹端改善（13→10 次）+ 方向与恢复力
假设一致。sp3>100 仅 1 次 → 剂量规则未触发，维持 0.1。
**定量结论：温和改善、未收敛**。质量维度（好丰富 vs 坏沙沙；小洞 mid
区是适度还是平淡）指标已到分辨率极限，裁决权移交用户肉眼——对比包
`user_w7_vs_w6b_20260919/`（8 对关键步：86K 过冲、88K/118K 逃逸、
100K/110K 小洞反转、102/106/112K 双丰富直比）。
后续选项：A 跑到 ~150K 复判（若仍不收窄→记录 W7 为涂抹端温和改善，
洞区质量走收割路线）；B 用户看包后定向（双丰富帧 W7 明显更好→继续/
考虑剂量；无差→减剂量 0.05 稳态或直接收割）。EMA 阻尼为下一 wave
候选（单变量原则，不与本 wave 混合）。

### 7.16 用户裁决 + 三臂好帧普查（2026-09-19）

**用户反馈（最高证据级）**：W7 小洞帧 99999/109999 仍有孔洞，与 W6
差不多；"偶尔出现的优秀结果可以很大程度上满足要求"——**模型有解决
问题的能力**（用户确认）；但扫 v19_w7 val 文件夹"没看到好结果"。

**三臂好帧普查（80K-132K，校准标准 tierA=tex≥9 & sp3≤60）**：
- W6-A: 4/25 (16%) — 82K(用户已认可)、106K、**126K、130K(最近!)**
- W6-B: 3/26 (12%) — 96K、110K、126K
- W7:   2/21 (10%) — **87999、117999（均为 166416=认可样本）**

**结论**：
1. 好模式在三臂都以 ~10-16% 频率间歇出现，从未消失——能力在，
   稳定性不在（用户自己的推断与诊断一致）；
2. W7 的洞区照片 D 没有提高好帧率（2/21 vs 12-16%），只温和改善了
   涂抹端——**本轮未修复核心问题（好模式稳定性）**，用户净体验
   无改善属实；
3. 待用户单比特裁决：W7 的 87999/117999 两帧（过校准标准、恰为
   认可样本）是好是差——好→W7 保住能力、走收割/播种路线；
   差→潜空间 D(64×64) 分辨率天花板嫌疑加重，指向像素级 patch D。
4. 路线：①收割+多种子播种（推理侧，立即可用，若固定好 ckpt 多种子
   采样好帧率显著高于权重漂移频率则系统可用）；②EMA 阻尼（下一
   wave）；③像素级洞区 patch D（重型，v14 NLayer D+mask 机器在库）。
   用户裁决 87999/117999 = **好**（校准标准第三次验证通过）；
   收割/播种被用户否决为"蠢"（治标）——要求机制级答案。

### 7.17 机制闭环：权重漂移定位 + 方向分析（2026-09-19，纯 CPU）

W7 五个 ckpt 恰构成完美相位序列 80K(好)→90K(涂抹)→100K(噪富)
→110K(好)→120K(涂抹)。相邻 ckpt 全参数相对漂移（/tmp/drift.py）：

| 区间 | bn_down | bn_mid | bn_up | attn | w_map | D |
|---|---|---|---|---|---|---|
| 80→90K | .0375 | .0389 | .0350 | .0144 | .0205 | .34 |
| 90→100K | .0373 | .0388 | .0336 | .0140 | .0194 | .25 |
| 100→110K | .0379 | .0401 | .0341 | .0139 | .0192 | .20 |
| 110→120K | .0378 | .0398 | .0338 | .0137 | .0195 | .25 |

**发现 1**：G 侧漂移速率恒定（每万步 ~3.4-4.0%），模块间均匀，模式
翻转处无任何脉冲/病灶/定位——不是"某个坏事件导致坏帧"。
**发现 2**（/tmp/drift_dir.py）：相邻位移向量方向余弦 = -0.041 /
-0.053 / -0.087——比随机方向期望(0.00004)大千倍但远小于 1：
**有界扩散 + 微弱回复**的特征（权重在平坦盆内随机漫游，偶尔碰壁折返）。

**机制最终版（全证据链）**：训练本身完全健康（无事故/无发散/无模块
失稳）；病根是损失面在"洞区高频纹理组织"维度近似平坦（照片参照监督
从未有效打在该维度——W7 潜空间 64×64 分辨率不足以约束像素级纹理
组织），SGD 噪声以恒定速率在平坦盆内做有界扩散，每 ~1 万步漫游过
一次模式边界；50 步 DDIM 把微小位移放大为肉眼好/坏翻转。**好帧=
扩散恰好落在好区域的时刻；坏帧不是发生了什么，而是没有任何力量
阻止它发生。**
修复对症排序：①像素级洞区 patch D（把受力真正打到可见纹理组织，
v14 NLayer D+mask_as_fake_target=原版像素 D 移植，在库）；②EMA
（恒速扩散→平均直接降方差；微弱回复力已被 cos<0 证实存在，EMA
放大稳态）；训练更久无效（扩散是平稳过程，骰子持续重掷）。

### 7.18 W8 提案：正确监督办法已在库中（v14 像素 D 完整复原，2026-09-19）

用户问"本地是否已有正确监督办法"——**是，且已逐行核实**：
- `_gan_fm_g_loss`（coach L1365-1400，"orig coach L1169-1198 faithful
  port"）：fake = pass1 x̂0 **像素解码**（`pred_x0_img_g`，L1786-1790
  现成），real = **源照片 x**；D 在 G 步冻结；adv(NonSaturatingWithR1)
  + FM；配对 detach 入队 D 步
- pass1 调用点 L1799 现存，仅被 `if not self.x0_enable` 关闭（W6/W7
  invsr 模式时代关闭）
- NLayer 像素 D + `_discriminator_step`（R1 gp 非惰性）+ 配置键全在
  （losses.adversarial.* / feature_matching.weight）
- **这就是 §7.11 认定的原版第三层**（adv×10/FM×100/real=照片/
  全帧），原版靠它产出干净洞区；W7 教训（潜空间 64×64 太粗）+
  机制闭环（§7.17 平坦维度）共同指向：把受力打到像素分辨率。

**W8 设计（预注册）**：
- 基底：W6-B@130K 分叉（运行中 W6-B = 同血统对照，单变量）；
- 新增：第二判别器 NLayer 像素 D 专管 pass1 洞区——fake=pass1 x̂0
  decode（t≤300 门），real=照片 x；adv×10 + FM×100（原版剂量；
  预注册风险：v14 曾因 Stage-B 梯度规范教训降至 FM×10——烟雾期盯
  grad_norms，不稳则 FM×100→×30→×10 阶梯回退）；
- 保留：W6-B 全部损失 + invsr 潜空间 D（p2/synth 对）原样；hole_d 关；
- 显存：+NLayer D ~1-2GB，烟雾验证 <23.5GB；
- 成功判据：tierA 好帧率在 +20K 步窗口内 > W6-B 同窗（16%）；
  振荡 std 收窄；判坏：grad 爆炸或可见区退化（W3-D 教训监控）。
- 待用户批准：停 W7（GPU2→W8）；W7 ckpt（80-120K）留盘。

### 7.19 勘误与致歉：W7 的"原版方案"标签名不副实（2026-09-19，用户质询）

用户指出矛盾："W7 就是你说原版监督方案，怎么 W8 也是"。核实后承认：
**W7 只复刻了原版的"配对语义"（fake=新视角输出、real=照片），其余
全部是 InvSR 组件**。真实对照表：

| 维度 | 原版 WarpGAN 第三层 | W7 实际实现 | W8 提案 |
|---|---|---|---|
| 判别介质 | 像素图（512） | **潜空间 latent 64×64** | 像素图 |
| D 结构 | NLayer patch D | InvSR UNet-D | NLayer（原版） |
| fake | pred_novel 完整输出 | x̂0 洞区掩码 latent | x̂0 像素解码 |
| real | 照片 x | 照片 latent（同掩码） | 照片 x |
| 损失类 | NonSaturating+R1+FM×100 | hinge、无 FM | 原版 |
| adv 剂量 | ×10 | **×0.1（弱 100 倍）** | ×10 |

W7 ≈ 20% 原版（配对）+ 80% InvSR（介质/结构/损失/剂量）。当时选择
潜空间实现的理由（复用已接线的 InvSR D、免 VAE 解码开销、gpt.md
处方）成立，但把它**标称为"原版方案复原"是我的错**——它没有复刻
原版起作用的那个部件（像素级判别+原版剂量）。40K 步的机会成本由
这个错误标签部分造成。
教训入流程：今后"参照复刻"类提案必须附上与参照的逐维度差异表
（本表格式），差异超过 2 项不得称"复原/复刻"。

### 7.20 W8 前全盘复盘：原版↔InvSR 理论连通 + bug 清单（2026-09-19）

**理论连通（两套第三层监督的统一）**：
- 原版约束"最终可见输出"（pred_novel vs 照片分布，像素分辨率，patch-D
  adv×10+FM×100）；InvSR 约束"训练路径上真正流动的量"（低 t 单步 x̂0，
  潜空间，t 条件 UNet-D，hinge+微 adv）——因为扩散无法廉价反传完整
  采样链。**两论的 fake 是同一个东西**（模型当前输出估计）；分歧仅在
  介质（像素 vs 潜空间），且各由其工程约束决定（FFC 无潜空间；InvSR
  免 VAE 解码）。
- W7 教训补完了桥：配对语义正确而介质错误 → 约束失效（64×64 latent
  装不下像素级纹理组织）。
- **W8 = 两论的并集**：InvSR 的量（x̂0，t<200 门）× 原版的介质与剂量
  （256px 解码 = 原版自身工作分辨率、NLayer patch-D、NonSaturating+R1、
  adv×10、FM×100、real=照片）。分工：UNet-D 保留 p2/synth 潜空间监督
  （W3-P/W6 已证有效），像素 D 专管 pass1 新视角（部署量）——
  参数不共享、指令互补，等价于原版多分支 D 拆成两个专职头。
- **与原版的全部偏差（预注册，仅 3 项）**：①fake=x̂0 单步估计(t<200)
  而非完整前向（扩散训练经济学，InvSR 已验证）；②只开 novel 分支
  （inv_warp 有 §7.12 重采样风险+覆盖率中位 8%；synth 已由潜空间 D
  服务）；③像素 D 预热 1000 步（原版从零训练无预热；我们是 130K
  预训练 G 上接入新鲜 D，直接 ×10 会追随机梯度）。

**Bug 清单（复盘发现）**：
1. **阻塞级（W8 场景必崩）**：init L477-516 顺序覆盖——同时开
   use_gan_fm 与 invsr 时，NLayer D 先建后被 UNet-D 静默覆盖（孤儿
   对象泄漏 GPU、optimizer_d 被抢），首次命中 `_gan_fm_g_loss` 会把
   3 通道像素图喂进 4 通道潜空间 UNet → 崩。W8 接线必须拆分为独立
   `pixel_discriminator`/`pixel_optimizer_d`/`_pixel_disc_queue`；
2. `_gan_fm_g_loss`/`_discriminator_step` 硬绑 self.discriminator/
   optimizer_d/_disc_queue —— 需参数化到像素 D 属性；
3. p1/p2 v14 调用点被 `if not self.x0_enable` 关闭 —— W8 需新门
   （x0_enable 下仅开 p1 像素对；p2 保持关，避免与潜空间 D 双重监督）；
4. 死配置：YAML losses.pixel.x0_clip=3.0 无人读取；解码路径实际用
   losses.x0.clip（默认 8.0）——W8 显式设 8；
5. save/resume 缺像素 D 键 —— 新增 'pixel_discriminator'/
   'pixel_optimizer_d'，resume 兼容旧 ckpt（无键→新鲜初始化）。
**复盘确认无恙项**：W7 hole_d 块（44K 步 0 崩溃+逻辑复查：掩码索引/
mode() 编码一致性/预热门/队列消费）；_predict_x0 α̅ 公式与调度器一致；
_decode_x0_to_pixel 归一化（/0.18215→decode→[0,1]）正确且 256px=
原版工作分辨率；_discriminator_step R1 非惰性逐对累积忠实原版；
_invsr() getattr 防护；NLayer 配置=原版逐字（3ch/ndf64/4 层/lr1e-4）；
NonSaturatingWithR1 签名与调用匹配；synth 从不入像素队列（pass1-only
范围天然成立）。

### 7.21 W8 实施与启动（2026-09-20，用户判定三问后批准执行）

用户判定标准：理论须①保上限②提下限③推理多好样本。判定依据：①纯
加法（W6-B 全部损失不动）；②直击 §7.17 平坦维度（像素分辨率照片
监督，W7 弱介质版已示方向有效）；③若权重漂移是载体（已证），钉住
好盆地后单种子好率应过半；判死线兜底。**执行**：
- 备份确认：W6-A/B 各有 iteration_0129999.pt + 代码标签双保险
  （v19-w6-baseline / v19-w7-state=9bebe37）；W7 停止（终值 ~124K，
  ckpt 80-120K 在盘）；
- §7.20 全部 5 项修复落地：双 D 拆分（pixel_discriminator/
  pixel_optimizer_d/_pixel_disc_queue，阻塞级覆盖 bug 消除）、
  _gan_fm_g_loss/_discriminator_step 参数化、p1 新门
  （`use_gan_fm and (not x0_enable or pixel_d_enable)`）、save/resume
  双 D 键（旧 ckpt 兼容：像素 D 新鲜初始化）、梯度范数双 D 汇总；
  adv 预热门（首个像素对起 +1000 步，legacy 语义不变）；
- 剂量=原版原值：adv×10 + FM×100，NLayer D 6.96M 参数，lr 1e-4，
  R1 gp 0.001，256px，t<200 门；
- 烟雾全绿：双 D 共存打印、p1_gen_fm=0.104 触发、p1_gen_adv 正确
  缺席（预热门）、D-step 像素对开火（fresh D real/fake≈相等符合
  预期）、UNet-D 恢复正常（real_p2/synth 路径不动）、0 Traceback、
  峰值显存 ~15GB；
- 正式启动：W6-B@130K 分叉续训 GPU2（v19_w8_pixeld_300k），运行中
  @130150 步起；W6-A@131.3K / W6-B@134.65K 对照不动；
- **判死线（重申）**：+15K 步（~145K）tierA 好帧率 ≤16%（W6-B 基线）
  或洞区无改善 → 停 W8，不再做第三个 D 变体，转 EMA+收割收尾；
  梯度爆炸或可见区退化（W3-D 病征）即停。

### 7.22 W8 事故报告与停机（2026-09-20，用户肉眼判定"整体恶化"→数据证实）

**两个即停条件同时触发，W8 已停**（终值 ~162K，用户指令前自主执行
预注册红线；130K 分叉 ckpt 完好，W6-A/B 对照不受影响）：

1. **梯度爆炸**：g_grad_abs 从基线 ~660（W6/W7 时代）→ adv 激活后
   34K→84K→168K→225K（50-340 倍）；
2. **可见区退化**：val_novel_visible 130K=0.0369（分叉基线）→
   132K=0.0659 → 159K=0.1278（同期 W6-B 稳定在 ~0.044）。

**因果链（时间对齐）**：像素 D 预热 1000 步结束 ≈131K，`p1_gen_adv`
首现 131150 步（值 9.6）——g_grad 立即跳 50 倍，2K 步内可见区开始
恶化，随后单调加剧至停机。adv 首现前 g_grad=664（正常）。

**归因（剂量，非机制）**：NonSaturatingWithR1.generator_loss 内部乘
self.weight（L116 `fake_loss.mean() * self.weight`）——adv×10 确已
生效；softplus(-D(fake))×10 对 130K 预训练扩散 G（从未见过 >×1 的
对抗梯度）过猛，且 fresh D 立即可分（novel 视角 vs 源视角照片），
梯度方向与新视角几何对抗，腐蚀全图。**原版剂量的语境不可移植**：
原版 FFC 从零训练、D/G 从第 0 步共同适应；我们把 ×10 接到预训练 G
+新鲜 D 上。**我们自己的配置注释早有警告**（feature_matching
"v14 starts at 1/10 — Stage-B lesson: gradual"）——被 §7.18"原版
原值"决策覆盖，此为本次事故的直接决策错误，责任在我。

**后续（待用户裁决，二选一）**：
- A. W8-lite 一次重试（§7.18 预注册阶梯的第一步回退，非新机制）：
  adv×1（InvSR 校准量级）+ FM×10（v14 渐进值=项目自身教训）+
  warmup 3000，同从 W6-B@130K 分叉；新增自动护栏：g_grad_abs >
  5×基线（~3500）即停；判定标准不变（+15K 步 tierA>16% 且可见区
  不退化）。
- B. 放弃像素 D（机制级），转收尾路线：EMA 阻尼 + 三臂至 300K +
  好帧收割——兑现"不再做 D 变体"承诺。

### 7.23 存档：用户校准结果原始版（2026-09-18 晚，82+15 张校准集）
> 本节写于 09-18，内容已被 §7.10（扩展版，含两臂同步性检验）吸收，保留备查。

用户裁决（`user_calibration_20260918/`）：
1. **w3p 文件夹**：只有高步数的一部分呈现"想要的效果+细碎小孔"，低步数
   呈油画感 → ①tex≥9 规则在 W3-P 上失效（缺成熟度门控）：实测 W3-P 的
   tex 从 3K 起就高（3K=11.2, 15K=9.2, 39K=11.2, 119K=9.5, 159K=7.7）——
   纹理"量"一直够，变的是纹理"种类"（笔刷感→照片毛孔感），能量指标测不出
   种类；②**长训假设获用户数据支持**：W3-P 质量随步数单调变好（L1 全程
   不可见），跑到 300K 有正当性。
2. **w6a/w6b 文件夹**：筛选结果基本符合预期，且两臂效果差不多 →
   ①tex≥9 规则在 W6 臂上成立（丰富端=用户偏好端确认）；
   ②adv×1.0 vs ×0.1 无可见差异——剂量对照使命终结（再次印证 §7.7 层2：
   D 不在洞区，剂量无意义）。
3. **细碎小孔**在所有"好"图上仍在（用户明示"加上细碎小孔"）——核心缺陷
   未自愈，W7 的靶。

**"为什么同一训练有的好有的差"的完整答案（两机制叠加）**：
- 长时间尺度（万步级）：单向成熟，油画→照片质感（正常训练进程）；
- 短时间尺度（2K 步级）：丰富↔涂抹/沙沙模式振荡（高频维度无参照锁定，
  §7.7 层3）；
- 好 = 成熟度够 ∧ 恰逢丰富模式；坏 = 欠成熟（早期油画）或模式塌落（涂抹/
  沙沙）。
- 修正后的筛选标准（v2）：好 = 步数/成熟度门控 ∧ tex≥9 ∧（小孔指标，
  待 W7 修复后收紧）。

**行动含义**：W3-P 继续至 300K（9/23）；W6-B 停训释放 GPU1 给 W7 冒烟
（待用户裁决）；好图收割 = 高步数+丰富模式的 checkpoint。

### 7.24 存档：三臂停止事件 + 振荡不衰减数据（2026-09-21 21:35 复盘）
> 本节写于找回丢失上下文**之前**：当时推断"人为停止"，后经用户确认实为
> **硬盘满**导致（详见 §7.25）。振荡不衰减的数据结论仍然有效（与 §7.17
> 平坦维度扩散机制互证）。

**停止事件**：三臂进程全部终止，非崩溃（零 Traceback、无 OOM、系统 11 天
未重启）。W3-P 停于 09-18 16:22（175K/300K）；W6-A/B 停于 09-21 17:02-03
（163.6K / 168K，同秒级停止=人为操作）。checkpoint 完好：W6A@160K、
W6B@160K、W3P@170K，丢失 ≤8K 步。

**振荡数据延伸（84K→165K，同样本+固定种子，speck3/tex）**：
- w6a 大洞样本：85K:60 → 87K:5 → 91K:72 → 99K:3 → 105K:58 → 115K:2 →
  139K:62 → 145K:3 → 157K:63（幅度不减）
- w6b 大洞样本：91K:70 → 99K:64 → 111K:78 → 115K:3 → 139K:46 → 157K:3 →
  165K:60
- **裁决：振荡不随训练衰减，160K+ 与 60K 时幅度相同（speck 2-78，
  tex 2.6-12.7）。结构性缺陷不会自愈，跑到 300K 也不会稳定。**
- 不稳定度与洞大小正相关（小洞样本基本稳定，偶发跳变）。

**当前决策窗口（GPU 全空）**：W3-P 长训假设已验证（质量随步数单调↑，
用户校准确认）、W6 剂量对照已终结（w6a≈w6b）、振荡已证实永久——
三臂的预注册问题全部有了答案。下一步的主选项：
1. **W7**（pass1 洞区照片 patch D + 修 synth real→照片）——唯一根治路径；
   可从 W6-A@160K 热启动（省 160K 步）或从零；
2. 多种子采样检验（判定好模式能否推理端锁定）——便宜，立即可做；
3. W3-P 是否续跑 175K→300K（边际收益 vs 5 天 GPU）；
4. W6-B 建议就此封档（与 W6-A 等效）。

### 7.8 流程教训（2026-09-18 用户质询入档，与 §6.2 同级）

用户指出：起跑前曾问"还缺什么"，agent 答"不缺"；跑三四天后又报"缺洞区
masked D"。核实书面记录：该处方确实从未出现在任何 wave 的预注册中——
不是上下文丢失导致遗忘，而是**起跑前未做"损失-靶点 vs 参考文献处方"的
逐条对照审计**。此为流程失败。

**新增硬性流程（W7 起生效）：任何新 wave 的预注册必须附"靶点映射表"——
每条 gpt.md 处方（§9 解耦监督/§11-12 masked patch D/§13 t 域/§27 消融顺序）
与 InvSR/参考项目的对应机制，逐条标注【已实现/有意不做（理由）/待做】，缺
一条不得起跑。**





### 7.25 上下文找回与当前待决状态（2026-09-21 深夜，硬盘满事故后）

**停止原因定案**：用户确认硬盘满是三臂+W8-lite+W9-EMA 全部停止的原因
（非人为选择停止）。磁盘现已恢复 413G 可用（experiments 共 131G，含大量
旧 wave 可归档）。**重启任何训练前必须先做磁盘治理 + 给脚本加 df 护栏。**

**丢失上下文中已发生且已从 git 历史完整找回的事件链**（§7.11-7.22）：
W7（潜空间洞区照片 D，W6-B@80K 分叉）→ 温和改善涂抹端但好帧率未升
（2/21 vs 12-16%）→ §7.17 机制闭环（平坦维度有界扩散）→ W8（原版像素 D
原剂量 adv×10）→ §7.22 剂量事故（梯度爆 50-340 倍，可见区腐蚀，红线停）
→ 双解药启动即被硬盘满杀死：
- **W8-lite**（GPU2，W6-B@160K 分叉，像素 D 校准剂量 adv×1+FM×10+
  warmup3000 + grad_guard.sh 自动护栏 g_grad>3500 即杀）：仅跑 ~100 步；
- **W9-EMA**（GPU1，W6-B 配方不动 + EMA 0.999，ema_state 入 ckpt）：
  启动即死（0 步）；未提交的 coach +54 行即 EMA 实现。

**恢复优先级（待用户裁决）**：①磁盘治理（旧 wave 归档/删除清单需批准）+
df 护栏；②复跑 W8-lite（机制级修复，判死线已在 §7.21 预注册）与
W9-EMA（纯稳定化，单变量对照 W6-B@160K）——两者不互斥，各占一卡；
③W6-A/W6-B/W3-P 维持停止（其预注册问题均已有答案）。

### 7.26 独立复核：前任诊断的验证结果（2026-09-21 深夜，用户要求不信任 AI 历史）

对 §7.11-7.22 逐项亲验（读代码/日志/原版配置，非转述）：
1. **振荡机制（§7.17"平坦维度+有界扩散"）：方向正确，但可具体化**。
   独立证据（本会话自算）：振幅 60K→165K 不减；两臂早期同步后期去相关；
   与训练窗口粗统计无相关。§7.17 的漂移率数字未独立复算（标注：未复验）。
   代码级具体化（新）：**拉锯战**——pass1 ε-MSE(全t全帧)的目标是
   anchor，其洞区被 5×5 中值滤波（anchor_median=5，kornia median_blur
   整图施加）→ 目标系统性缺纹理 = 持续教洞区"糊"；真实纹理的唯一来源
   是冻结 SD 先验经 BrushNet/注意力门控。两力近似平衡 → 净梯度≈0 →
   门控强度随机漫步 → 采样器放大为双峰输出。W3-P（无 median）锚点洞区
   含重投影噪声 → 教"小孔"而非"糊"——解释其 0 涂抹帧+"好+小孔"。
2. **W8 归因（"剂量非机制"）：核实成立**。亲验：adv×10+FM×100 确在
   脚本；g_grad 事故现场 101,754/84,603/237,920（基线~660）恰在 adv
   激活后；可见区 0.036→0.128 单调恶化。**修正一个前任未查实的点**：
   原版 mask_as_fake_target=${with_mask} 而 with_mask: **False**——原版
   同样全帧无掩码对抗，我们的 mask=None 移植在这一点上**忠实**，非 bug。
   爆炸机制具体化：新鲜 D 以视角差立即可分 → ×10 梯度方向与新视角
   几何对抗 → 腐蚀全图；原版从零共训消化了同样的 shortcut。
3. **W7 失败归因（"潜空间介质太粗"）：混淆实验，不能定论**。W7 与原版
   同时差 4 项：介质(潜 64×64 vs 像素)、剂量(×0.1 vs ×10)、无 FM、
   仅 40K 步。欠功率假设同样成立（0.1 的 adv 对抗 ε-MSE×1+LPIPS×2 的
   糊向拉力本就不足）。
4. **W8-lite / W9-EMA：非失败，未测**（硬盘满杀死在 ~0 步，判据已预注册）。
5. 修复路径（复核后排序）：①W8-lite 复跑（剂量假设的直接检验+护栏）；
   ②EMA 并行；③**新增提案（本复核产出）：选择性去斑锚点**——median5
   无差别抹掉小孔与真实纹理，改为仅对检测到的孤立脉冲点取中值
   （保住发丝等有组织高频），让 ε-MSE 目标本身变成"纹理正确"的，
   从根上拆掉拉锯战的糊向力，零 GAN 风险，实现成本低（仅目标构造）。

### 7.27 五支柱溯源审计（2026-09-21 深夜，用户要求逐一定位痕迹并验证忠实度）

| 支柱 | 痕迹 | 忠实度核验（亲验行级） | 偏差状态 |
|---|---|---|---|
| 原版WarpGAN | utils/warp/*=原文件；train()交替=orig L489-560 ✓；双pass=L207-305 ✓；_gan_fm_g_loss=L1160-1198 逐行 ✓（含 with_mask:False 使 mask 失效——全帧对抗与原版一致）；codes→生成器（orig ws=codes 亲验）| 核心移植忠实 | ①真实批 novel 分支 D 移植时丢失（§7.11 已发现，W7/W8 修复中）；②anchor_median 为我方新增（已标注）；③D 配对重指向（§7.7）——均已入档 |
| InvSR | 源码入库 reference/InvSR-master；backward_step L1239 ✓；_Latent_bound±10=L43/L1288 ✓；hinge=L1611 ✓；ema_rate=L129 ✓ | 四件套行级一致 | **新发现**：InvSR 训练 t=[100,150,200,250] 离散4值（sd-turbo-sr-ldis.yaml L252），我方 t≤300 连续——文档只提了域未提离散性；方向有利（覆盖更全），无碍 |
| BrushNet | diffusers BrushNetModel 官方类；init=pretrained(官方ckpt)或from_unet(官方从零配方, 5ch/'rgb') | 官方配方 ✓ | 我方设计：condition 预填充（渲染填洞）非标准黑洞 inpainting 用法；mask 通道仍标注洞位——合理设计选择，此前未显式文档化 |
| ReferenceNet | 范式源在场 models/referencenet/mutual_self_attention.py(ReferenceAttentionControl)；实现=自有 attention_processor.py(bank+门控) | 范式级（docstring 诚实标注 NOT imported）| 目录含 moondream 系残件未用 |
| W+ | orig 把 codes 喂 inpaintor（ws=codes 亲验）；我方 WProjModel→attn2 tokens | 机制保留、介质适配（FFC→cross-attn）| WProjModel 为我方修复版（IP-Adapter 式投影）|

### 7.28 振荡解剖：单一机制的完整推导（2026-09-21 深夜，回应用户"并列修复=诊断含混"的质疑）

方法：解析 81 张 W6-A val PNG（2K-162K 每 2K）+ 83 张 W6-B 的六联版面
（x|yhat|cond|mask|anchor|gen），零模型加载。产物在
experiments/analysis_osc_20260921/（filmstrip_w6a.png / hole_zoom_examples.png /
timeline_spectra.png / timeline_data.csv / summary.txt）。

新事实（全部可复算，脚本 /tmp/osc_anatomy.py 与 /tmp/p4probe.py）：
1. val 采样 seed=42 固定 + 条件固定 → 模式翻转 100% 由权重变化驱动（代码亲验 L2807-2836）。
2. 质量轴 = 洞区频谱组织性（slope，r∈[32,120] 线性拟合）：双峰
   -0.008~-0.016（浅谱=有纹理）vs -0.029~-0.032（陡谱=涂抹）；
   用户亲判好帧 129999(-0.0084) / 147999(-0.0130) 均落浅谱群。
   频谱形状验证：浅谱模式 HF-LF 带功率差 -1.71 ≈ 照片渲染 -1.57；
   涂抹模式 -2.95（高频崩塌）。Laplacian 总能量不能分类（沙沙也有能量，
   首版分类器 81 帧全判好——教训记入）。
3. P1 证实：anchor 洞区纹理能量 = 照片渲染的 0.58 倍 → ε-MSE 目标本身缺纹理。
4. 两种模式能量均超照片渲染 1.5-7× → ε-MSE 连能量尺度都未锁住该轴。
5. ★关键证据：W6-A/B 模式同步率 0.99（early 0.98 → late 1.00，不降反升）
   + 训练 SEED 固定 + bs=1 + real/synth 每 2 步交替
   → 翻转是共享数据流的确定性响应（相干游走/极限环），
   否决"独立权重扩散"表述。翻转统计：A 臂 40 次/80 区间（~4K 步周期量级），
   好率 65% 无衰减（halves 0.68/0.63），最长好/坏连段 22K/8K。
6. 样本0个人好率 65% ≠ 全 val 集 10-16%（此前口径为多样本）——不矛盾，
   待用户图视校准 slope 阈值与"假好"（高能白噪声）后重算全量。

修正后的因果拓扑（单一机制，两个必要条件）：
  使能者：洞区"照片纹理组织"轴上无有效回复力
    （anchor 离流形 0.58× + 无分布级补偿——§7.11 的移植缺口）
  驱动者：共享数据流的相干推拉（同种子同序，bs=1 交替采样）
  二者缺一不振：有回复力则任何驱动都被阻尼；无驱动则相干翻转无从谈起。
  我此前把修复并列呈现是拓扑错误。正确分类：
  - 断使能者=治本：A 选择性去斑锚点（修目标）或 B W8-lite（加分布约束，
    原版第三层的方案）——同一环节的两种修法，可单选可叠加
  - 断驱动者显影=治标/诊断：C EMA（平均掉 2-4K 周期分量，τ≈1K 步，
    对 4K 周期只部分有效）；D 改数据种子（换相位的鉴别实验，1 卡便宜）
待用户对三张图的二次反馈校准分类阈值，然后重算全量好帧率曲线。

### 7.29 用户二次反馈落地：三模式分类器 v2 + 接缝假说证实（2026-09-21）

用户图视反馈（对 §7.28 三图）：
1. anchor 与渲染行视觉无差（0.58× 能量差肉眼难辨）；
2. 陡谱组=涂抹 ✓；用户好帧组=最真实但欠自然 ✓；高能组=油画感过锐（非沙沙）；
3. ★用户假说：沙沙感来自 hole/visible 对齐不均匀产生的单像素孔洞；
4. 绿框多假好（脸上孔洞/色斑），真合格"少之又少"。

验证（/tmp/v2probe.py, /tmp/v2figs.py，零训练成本）：
- ★孔洞定位：边界带(洞∩可见外扩4px) 21.2/kpx vs 洞内部 1.0/kpx
  —— 22× 集中于接缝，用户假说结构性证实；
- ★输入源审计：cond 可见区 warp = 36.4 ph/kpx（唯一脏输入），
  anchor=0.28，渲染=0.20 → 接缝伪影由模型复制输入 warp 的 splat 孔；
- corr(ph,tex)=0.91：孔洞与高能模式共生 → SPECKLED 类涵盖"沙沙+锐利"复合；
- 色斑检测器未校准（干净渲染底噪≈20/kpx），暂不参与分类；
- 分类 v2（PH0=16.9 由用户两好帧×1.25 校准，slope<-0.02=涂抹，
  ph>16.9=孔洞，tex>0.04=过锐）：GOOD 38 / SMEAR 28 / SPECKLED 15 / SHARP 0，
  翻转 54 次/80 区间。GOOD=47% 仍高于用户目测——待第三轮图视校准
  （filmstrip_v2.png / seam_zoom_v2.png 已产出）。

最终因果结构（两处独立缺陷位，回答"并列修复"质疑的最终版）：
  缺陷位1（输入侧）：cond 可见 warp 携带接缝孔洞 → SPECKLED 模式
    （修复=选择性去斑 cond 可见区，train+infer 同契约）
  缺陷位2（监督侧）：纹理组织轴无回复力（§7.28）→ 轴随数据流游走
    → 低端=涂抹，高端=过锐/孔洞放大，中间窄窗=好
    （修复=A 锚点去斑 或 B W8-lite 分布约束）
  好帧 = 两缺陷位同时不发作 —— 这解释"少之又少"。
判定标准（预注册）：复合验证器 v2 为准；
  W10-cond 去斑 fork(W6-A@160K, 10-20K 步)：ph_ring<2/kpx 且他轴不变 → 缺陷位1证实；
  A/B 任一：slope 分布钉在 ≥-0.02 带无穿越 → 缺陷位2证实；
### 7.30 从用户标注学出验证器：诊断反转 + 证据链闭合（2026-09-22 凌晨）

流程升级（回应用户"猜测太慢"）：停猜测，学标准。工具 label_tool.html，
用户标注 42/81 帧（14G/28B，39 跳过）→ 37 特征（频谱16bin+纹理+孔洞+颜色+
相干性+接缝对比）正则逻辑回归，留一交叉验证。

验证器：LOO acc=0.81 AUC=0.85（C=0.01）。单特征 AUC（无选择偏倚）：
  ph_vis=0.95（可见区保真度!）seam=0.89 lstd=0.86 lskew(反)=0.77
  ph_ring=0.77 cs1=0.71 ... slope 仅 0.5x（权重 0.03）

★诊断反转（v2 分类器方向错误的原因）：用户的好/坏轴 = **全局保真态**，
非洞区谱组织、非孔洞数量（孔洞与"好"正相关——保真模式连 cond 的 speckle
都保留；油画帧 ph_vis=82 仍坏 = 高频过冲，98k 陡谱帧 ph_vis=117 却好）。
好坏 = 同一游走轴的欠冲（平滑/涂抹）与过冲（不自然高频/油画），
好 = 窄窗。验证器好率时间线（20K bin）：7/10,3,4,4,5,1,3,4,0/1 —— 无学习趋势。

★证据链最终闭合（三环互锁）：
  代码环：L1663 anchor 整图 median5 + L1804 pass1 ε-MSE 无 mask（全帧）
        → 监督教全图平滑（可见区也糊），与 cond-copy 拉锯
  测量环：anchor 全图纹理=照片 0.58×；好帧 ph_vis/seam/lstd 高=保真态
  动力学环：两力近抵消→轴平坦→共享数据流驱动游走（sync 0.99）
  交叉验证环：W3-P(无median) 可见目标干净→可见保真稳定→"好+小孔0涂抹"✓；
              W6(整图median) 全图平滑模式出现→涂抹帧出现 ✓ —— 与历史校准记忆吻合

最终修复 A（证据锁定形态）：
  anchor 可见区 = 原像素精确拷贝（撤销整图 median 对可见区的污染）
  anchor 洞区 = 选择性脉冲去斑（保留纹理，只杀重投影孔）
  → 两侧拉锯均变同向：可见区"复制即对"，洞区"纹理正确"
预注册判据（fork W6-A@160K，20K 步）：
  验证器 P(G) 好率 ≥50%（现预测 38%）且 P(G) 方差下降（稳定化）；
  40K 步 ≥60%；可见区 L1 恶化>20% 或梯度>10× 即停。
验证器局限（诚实）：42 标签、LOO 0.81、跳过帧预测多在 0.25-0.67 模糊带；
后续可用 39 跳过帧+多样本标注增强。文件：/tmp/feats.npz /tmp/validator.npz
### 7.31 W10 修复上线：三臂并跑（2026-09-22 02:00）

代码改动（已验证编译+冒烟通过）：
1. coach L238-249：新增 anchor_clean 模式（whole=legacy / selective_hole=W10）
   与 anchor_despeckle_tau=0.07 两个配置项
2. coach _build_novel_view L1673-1688：selective_hole 分支——可见区原样不动
   （复制契约恢复），洞区仅替换孤立脉冲像素（|anchor-median|>tau，amax 通道）
3. coach checkpoint_me：checkpoint 轮转（log.max_ckpt_keep=2，默认 2）
   —— 硬盘满事故结构性防再发
4. yaml 默认值：anchor_clean=whole（向后兼容，W6 复现不受影响）+ max_ckpt_keep=2
5. 脚本：v19_w10_anchorclean.sh(GPU0)、v19_w10c_anchorclean_ema.sh(GPU2)、
   v19_w10_monitor.sh(30min 周期)；validator 资产入库
   experiments/analysis_osc_20260921/（validator.npz 含 intercept 重存，
   feats.npz, 全部分析脚本）；打分器 scripts/score_val_frames.py +
   scripts/validator_feats.py（对 W6-A 全量打分与训练时预测一致已验证）

预览（用户确认材料）：train_logs/anchor_cleanup_full.png
（photo|render|mask|anchor_raw|med5_LEGACY|selective_W10 六列）+
anchor_cleanup_crops.png（洞区 2x：raw|legacy|W10|差异×5）。
脉冲检出 7003-23547 px/样本（≈1-3% 像素为洞区孤立噪点）。

事故记录（诚实）：W10 首次冒烟未发现 train 脚本会从 checkpoint 路径推导
exp_dir（L47-50），resume 直接指向 W6-A 目录——结果覆盖了 W6-A 的
val_step0159999.png（历史 val 图一张，157999/161999 幸存，指标在 TB 中）
并短暂写入 iteration_0160001.pt（已删除；轮转保 2，无历史 ckpt 损失）。
已修复 fork 脚本（先复制基座 ckpt 进自己目录再 resume），W10C/EMA 未受影响。

三臂布局（全部 fork 自 W6-A@160K 单变量对照，EMA 控制组 fork 自其自身目录）：
  GPU0 W10   = W6-A 配方 + anchor_clean=selective_hole（§7.30 修复组）
  GPU2 W10C  = W10 + ema.rate=0.999（修复+稳定组）
  GPU1 EMA   = W6-B 配方 + ema.rate=0.999（纯稳定对照组）
  全部 max_steps=179999（20K 步判死线窗口），max_ckpt_keep=2，
  val 每 2000 步存 PNG，监控 30min 打验证器分 + df
冒烟审计：PASS（峰值 23.08GiB<23.5）；W10 首段损失健康
（real_p1_eps 0.06-0.18 无爆炸）

判死线（§7.30 预注册）：+20K 步内验证器好率 ≥50%（W6-A 基线预测 38%）
且 P(G) 方差下降；40K ≥60%；可见区 L1 恶化>20% 或梯度>10× 即停。
验证器局限不变（42 标签 LOO 0.81）。

### 7.32 W6 全套代码备份确认（2026-09-22，用户指令）

- W6-A/B 运行基线已冻结于 git 40c69df（2026-09-18，提交信息明示
  "code frozen as W6-A/B run base before W7"）；逐文件 git show 验证包含
  coach / train 脚本 / v19_w6a.sh / v19_w6b.sh / 主配置 / 数据集类 / warp 算子。
- 实体备份 backups/w6_full_code_backup_20260922/（2955 文件，223M）+
  .tar.gz（183M，sha256 ae77842992492efbccc3…）：
  git_commit_40c69df/ 完整源码树 + w6a/b_frozen_config.yaml（实验目录冻结配置）
  + w6_launch_scripts/（40c69df 版）+ coach_diff_40c69df_to_w10.patch
  （证明 legacy median 数学未变，anchor_clean=whole 与基线等价）+ README 复现步骤。
- git 固化：fc085c4（备份目录与库内对象哈希相同≈零存储成本；tar.gz 不入库）。
- 提醒：备份与仓库同盘；origin(github) 在但未推送，异地容灾需 git push
  （PUSH_GUIDE.md 流程）。
