# PREREG_V18W1 — v18 wave-1 三臂对照实验预注册（起跑前签署）

> 写于 2026-09-09 深夜，任何臂起跑之前。本文件预注册：假设、配方、判据、
> 决策地图、止损规则。训练开始后本文件不再修改；结果无论如何都按 §5 决策
> 地图行动，不进入"继续盲调"。

## 1. 背景（一段话）

诊断（代码+数据三方核实）：原版 FFC 的空洞质感来自 pass2 位置全帧照片分布
监督（adv×10+FM×100@512px 直连输出）；diffusion 移植把该族损失整体关闭后，
ε-MSE 只教结构与能量、教不出"什么内容配什么质感"，空洞区高频退化为 SD
先验平均统计（"沙沙感"，能量 0.029≈照片 0.0296，但内容错误；FFC 能量仅
0.0104≈渲染 0.0081 但内容自适应）。本轮用三个同基座臂把"剂量"与"结构"
两个假设分开裁决。

## 2. 三臂（GPU 1/2/3 并行，各 50,000 步，从零同数据同代码）

共用基座（三臂完全一致）：v17-prime 结构（teacher ①④ 语义已锁定）+
P1 修复（synth 判别对=同视角像素配对：real=加噪 target）+ FM/adv 全族
ᾱ_t-SNR 加权（净 ε 梯度因子 ᾱ·√(1−ᾱ)）+ 原生分辨率 patch 解码
（32 latent 随机裁块+4px 上下文，替代 64→32 latent 降采样）+ W+ 老师阶梯
第一步 s1_mapper 从零训练（只在 synth 批更新，红线保持）+ 条件软化
erode3/blur21 + 全时间步 [0,1000) + bs=1。

| 臂 | GPU | 相对基座的唯一变量 | 回答的问题 |
|---|---|---|---|
| CTRL | 1 | 分布监督全关（纯 ε-MSE；pass1 经 hole_weight=1.0 实现全带锚） | 因果对照：FULL 的任何效果归因于分布监督族 |
| FULL | 2 | 分布监督开满：x0-L1(ᾱ)×1 + FM×100 + adv×10，全族 ᾱ 加权 | **剂量假设**：原版密度的分布监督是否把盲区内容拉向照片 |
| STRUCT | 3 | FULL + 真实批 pass1 前向删除 + pass2 bank=[warp_warp, x_mirror 照片] | **结构假设**：不再教盲区渲染，盲区能力是否如 FFC 般从 pass2 迁移 |

## 3. 已知风险与止损（起跑前写死）

- R-a D 饱和/捷径：p2/x0_discr_real_out 或 fake_out 连续 >0.95 或 <0.05
  超过 3 次巡检（30 分钟）→ 判 D 捷径，停 FULL/STRUCT，先查日志归因
  （v14 类事故），未归因不重启。
- R-b 发散：real_p2_gen_fm 或 discr_adv 相对首小时均值放大 >10× 且
  g_grad_abs 同步爆炸 → 停臂，按 §5 分支处理（降 adv 重启为预注册动作，
  非盲调）。
- R-c 显存：冒烟峰值 >23.5 GiB 即停（24G 卡红线，留 0.5G 余量）。
- R-d s1 稳定性：W+ mapper 冒烟须显示 mapper 梯度只在 synth 步非零；
  若 5K 步内 synth_eps 不降反升且肉眼面板崩坏 → 三臂统一回退
  wplus.mode=off 重跑（预注册的降级动作）。

## 4. 判据（终点=50K；中期检查=10K、25K）

自动指标（辅助，不裁决）：eval_anchor_compare 重跑（同 24 样例 seed=42）：
空洞 L1、空洞拉普拉斯能量方向；val_novel_full/hole 曲线。
**主终点（裁决）=用户肉眼强制三选一**（每样例）："盲区质感更像
①真实照片/此人 ②FFC 式自适应平滑 ③沙沙/错误内容"。裁决集=固定 8 身份
×3 视角（含 h≥0.3 大洞子集），FFC/CTRL/FULL/STRUCT 并排+空洞放大裁块。
免费诊断：胜者臂 DDIM 10/25/50/100 步扫描（质感随步数显著变化 ⇒
训练/推理差距是活约束）。

## 5. 决策地图（穷举，无"继续调"出口）

| 结果 | 结论 | 下一步（wave 2，预注册） |
|---|---|---|
| FULL>CTRL 且肉眼明确（大洞子集≥3/8 朝①/②移动） | 剂量假设成立，方向活 | 胜者长跑 100K 准备；STRUCT 若同优则 STRUCT 为基座 |
| FULL≈CTRL 且 D 健康 | 该构造分布监督无效 | 看 STRUCT：STRUCT 好⇒结构是元凶；均平⇒约束在框架层（512px patch-FM / 采样链 / W+ s2），按 DDIM 扫描结果排序 |
| FULL/STRUCT 发散 | GAN 稳定性问题（非方向问题） | 预注册降级：adv=0 FM×100 重启同臂（单变量），仍答剂量问题 |
| STRUCT 几何崩坏（大洞五官错乱） | pass1 锚在做几何工作 | 混合结构：STRUCT+pass1 仅低频锚（v12 资产）为 wave-2 臂 |
| CTRL 意外大幅变好 | 基座改动本身有效 | 暂停解释，向用户汇报重归因 |
| s1 W+ 崩（R-d） | 老师阶梯第一档不稳 | 三臂统一 wplus.mode=off 重跑；s1 单独小步数排查 |

## 6. 诚实边界（本轮不回答）

512px 监督分辨率天花板；100K+ 预算效应；W+ s2_qkv；与 v12/v16 的严格
同机对照（其 checkpoint 未迁移，参照用 anchor_compare 档案数据）。

## 7. 产物位置

- 训练日志：`train_logs/v18w1_{ctrl,full,struct}.log`
- 健康巡检：`train_logs/v18w1_health.status`（每 10 分钟追加）
- 验证面板：`experiments/train_inpainting_diffusion/v18w1_*_50k/logs/images/val/`
- TensorBoard：同目录 `logs/`
- 终评：`experiments/anchor_compare_v18w1/`（50K 后由
  `scripts/eval_anchor_compare.py` 生成）

## 8. 签署

预注册完成，按用户 2026-09-09 授权（"修改代码、直接开启训练、50K 步、
拒绝低剂量/自定阈值妥协"）执行。起跑时间见各 log 首行。
